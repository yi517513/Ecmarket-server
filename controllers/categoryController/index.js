const { createCategory } = require("./createCategory");
const { deleteCategory } = require("./deleteCategory");
const { getCategoies } = require("./getCategoies");
const { updateCategory } = require("./updateCategory");

module.exports = {
  createCategory,
  deleteCategory,
  getCategoies,
  updateCategory,
};
